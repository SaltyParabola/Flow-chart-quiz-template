# Flow-chart-quiz-template
Each question/step in the flowchart is an object. The output field of each object is the name of the next object, so the function to string them all together is very minimal, as each object calls the next one.
